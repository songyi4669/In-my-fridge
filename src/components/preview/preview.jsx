import React from 'react';
import styles from './preview.module.css';

const Preview = props => (
    <section className={styles.preview}>
        <h1 className={styles.title}>냉장고 보기</h1>
    </section>
);

export default Preview;